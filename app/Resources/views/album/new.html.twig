{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="text-center titretable">Création d'équipe</h1>
    <div class=" container well">
    {{ form_start(form) }}
        <div class="row">
            <div class="form-group">
                {{ form_label(form.titreAlbum, "Titre/album", {'label_attr': {'class': ''}}) }}
                <div class="">
                    {{ form_widget(form.titreAlbum, {'attr': {'class': 'form-control', 'placeholder': "Titre/album"}}) }}
                </div>
            </div>
        </div>
        <div class="row">
            <div class="form-group">
                {{ form_label(form.artiste, "Artiste", {'label_attr': {'class': ''}}) }}
                <div class="">
                    {{ form_widget(form.artiste, {'attr': {'class': 'form-control', 'placeholder': "Artiste"}}) }}
                </div>
            </div>
        </div>
        <div class="row">
            <div class="form-group">
                {{ form_label(form.genre, "Genre", {'label_attr': {'class': ''}}) }}
                <div class="">
                    {{ form_widget(form.genre, {'attr': {'class': 'form-control', 'placeholder': "Genre"}}) }}
                </div>
            </div>
        </div>
        <div class="row">
            <div class="form-group">
                {{ form_label(form.support, "Support", {'label_attr': {'class': ''}}) }}
                <div class="">
                    {{ form_widget(form.support, {'attr': {'class': 'form-control', 'placeholder': "Support"}}) }}
                </div>
            </div>
        </div>
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

        <button type="submit" class="btn btn-default">Submit</button>
        <a href="{{ path('_index') }}" class="btn btn-sucess">Back to the list</a>
    </div>
{% endblock %}
